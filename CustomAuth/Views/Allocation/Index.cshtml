@model IEnumerable<CW.ViewModels.AllocationViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AllocationValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AllocationDate)
        </th>
        <th>
            Exemplars count
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AllocationValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AllocationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExemplarsCount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllocationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllocationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllocationId })
        </td>
    </tr>
}

</table>
