@model IEnumerable<DalToWeb.Models.EditionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EditionTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EditionTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EditionTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EditionTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EditionTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EditionTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EditionTypeId })
        </td>
    </tr>
}

</table>
